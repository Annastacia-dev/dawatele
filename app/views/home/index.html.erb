<% if !user_signed_in? %>
 <%= render 'landing_page' %>
<% else %>
  <div>
    <h1 class="font-bold text-xl">Hello <%= current_user.first_name %> (<%= current_user.role.humanize %>),</h1>
  </div>
<% end %>
