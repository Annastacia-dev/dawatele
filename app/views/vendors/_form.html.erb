<%= form_with(model: vendor, class: "contents") do |form| %>
  <%= render 'shared/error_messages', object: vendor %>

  <div class="flex flex-col gap-6 p-4">
    <div class='flex flex-col gap-1'>
      <%= form.label :name, class: "uppercase text-sm font-medium" %>
      <%= form.text_field :name, class: 'bg-transparent border border-gray-300 p-3 outline-none w-60 focus:ring-transparent focus:outline-none shadow-none',
      required: true %>
    </div>
    <div class="inline mt-4">
      <%= form.submit class: "rounded py-3 px-5 bg-primary text-white inline-block font-medium cursor-pointer border-none outline-none uppercase text-sm" %>
    </div>
  </div>
<% end %>
